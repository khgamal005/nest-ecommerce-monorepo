{
  "name": "ecommerce",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev:user-web": "nx dev user-web",
    "dev:admin-dashboard": "nx dev admin-dashboard",
    "dev:backend": "nx serve backend",
    "build": "nx run-many --target=build --all",
    "test": "nx run-many --target=test --all",
    "lint": "nx run-many --target=lint --all",
    "graph": "nx graph"
  },
  "devDependencies": {
    "@nx/js": "^23.1.0",
    "@nx/nest": "^23.1.0",
    "@nx/next": "^23.1.0",
    "@types/bcrypt": "^6.0.0",
    "@types/cookie-parser": "^1.4.10",
    "nx": "^23.1.0",
    "typescript": "^7.0.2"
  }
}
