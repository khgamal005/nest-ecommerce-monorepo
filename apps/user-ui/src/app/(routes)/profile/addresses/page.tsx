'use client';

import ProfileShippingAddress from '../_components/ProfileShippingAddress';

const ProfileAddressesPage = () => {
  return <ProfileShippingAddress />;
};

export default ProfileAddressesPage;
